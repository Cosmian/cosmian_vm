---
name: Release

on:
  workflow_call:

jobs:
  release:
    name: Release binaries
    runs-on: [self-hosted, no-tee]

    steps:
      - name: Download binaries
        uses: actions/download-artifact@v4

      - name: Display artifacts content
        run: find .

      - name: Release on tags, attach asset on release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ./cosmian_vm_ubuntu-22.04/target/debian/cosmian-vm-agent-ubuntu-22.04_1.2.3-1_amd64.deb
            ./cosmian_vm_ubuntu-22.04/target/debian/cosmian-vm-ubuntu-22.04_1.2.3-1_amd64.deb
            ./cosmian_vm_ubuntu-22.04/target/generate-rpm/cosmian_vm-1.2.3-1.x86_64.rpm
            ./cosmian_vm_ubuntu-22.04/target/generate-rpm/cosmian_vm_agent-1.2.3-1.x86_64.rpm
            ./cosmian_vm_ubuntu-24.04/target/debian/cosmian-vm-ubuntu-24.04_1.2.3-1_amd64.deb
            ./cosmian_vm_ubuntu-24.04/target/debian/cosmian-vm-agent-ubuntu-24.04_1.2.3-1_amd64.deb

  cleanup:
    name: Clean Github cache
    if: startsWith(github.ref, 'refs/tags/')
    uses: Cosmian/reusable_workflows/.github/workflows/cleanup_cache.yml@main
    secrets: inherit
